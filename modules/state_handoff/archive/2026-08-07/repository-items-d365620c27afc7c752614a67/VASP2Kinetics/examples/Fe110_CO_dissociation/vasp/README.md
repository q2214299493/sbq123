# VASP input placeholder

Place an existing, completed VASP or NEB result directory here. Required files
depend on the calculation type and include real `OUTCAR` data. This repository
does not generate or modify VASP files.
