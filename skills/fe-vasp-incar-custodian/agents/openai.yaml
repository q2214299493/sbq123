interface:
  display_name: "Fe VASP INCAR Custodian"
  short_description: "Diagnose Fe VASP INCAR inputs read-only by default"
  default_prompt: "Use $fe-vasp-incar-custodian to diagnose this Fe-based VASP run in read-only mode and persist a recommendation only when explicitly requested."
