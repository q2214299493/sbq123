# SCF9753825: exclude gknew0440 after MPI startup failure

Checkpoint: 2026-09-13 Asia/Shanghai. Scheduler RUN means allocation only.

- User replied “可以” to stop stalled SCF9753658, preserve outputs and submit
  one identical 80-core save-state SCF excluding gknew0440.
- SCF9753658 is confirmed EXIT. Previous inspection found 32+32 waiting VASP
  ranks on gknew0233/gknew0627, no proxy/ranks on gknew0440, and no SCF output.
  This establishes incomplete MPI launch, not an SCF convergence failure;
  the underlying launcher/node service fault remains unconfirmed.
- New SCF9753825 is RUN in Gkn_normal. Recorded resource request is
  `select[hname!=gknew0440] span[ptile=32]`; allocation is gknew0421:32,
  gknew0447:32, gknew0716:16. The excluded node is absent.
- Two post-allocation checks found zero user VASP ranks on all three nodes,
  and no vasp.out/OUTCAR/OSZICAR. `bpeek 9753825` reported “is starting.”
  MPI startup is therefore NOT yet verified. No electronic steps, convergence,
  saved restart state, migration TS or barrier is claimed.
- INCAR, KPOINTS, POSCAR and POTCAR.spec are byte-identical to SCF9753658;
  ALGO Normal, 80 ranks, EDIFF1e-7, NELM200, LWAVE/LCHARG=True and all physical
  settings are preserved. The one changed input is script.lsf's node constraint.

The canonical submission executor now parses one hash-bound `#BSUB -R`
directive, rejects malformed/duplicate/other directives, and explicitly passes
the resource string as one shell-quoted bsub argument. Inputs and authorization
are revalidated before submission. No separate submission executor was created.

The first replacement directory `h_migration_int06_mid_scf_no0440_20260913`
was rejected by bsub before a job was created: the site esub wrapper could not
preserve double quotes around the hostname. Its reservation, rejection
reconciliation and input bundle remain preserved. A read-only lshosts query
accepted the unquoted-host expression and excluded gknew0440. The optional
bsub -Zs dry-run failed on the site's inaccessible JOB_SPOOLDIR; it was not a
successful syntax validation. A fresh bound v2 directory then submitted
successfully, and bjobs confirmed the effective resource restriction.
Resource syntax reference: [IBM LSF resource requirements](https://www.ibm.com/docs/en/spectrum-lsf/10.1.0?topic=requirements-resource-requirement-strings).

Validation: root runtime submission/lifecycle/execution-gate suites 110 passed;
release-history compatible submission/gate suites 46 passed; targeted Ruff and
Python compilation passed. No VASP job was launched as a software test.
The source change is ported narrowly to the release branch's existing interfaces;
unrelated root workflow changes are not imported.

Next: verify actual startup of all 80 ranks and electronic output. Then require
normal SCF completion, EDIFF convergence, unchanged geometry, and valid completed
WAVECAR/CHGCAR before any restart verification or short Dimer trial. No additional
retry or Dimer was submitted. Prior accepted INT06 and MID-to-FS O-H TS stay unchanged.
