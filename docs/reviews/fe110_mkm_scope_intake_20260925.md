# Fe(110) 微观动力学第一步：范围与 CATKINAS 输入盘点

状态：`DRAFT_FOR_CONDITIONS_AND_NETWORK_REVIEW`。用户已选择先研究 C₂H₂O 路线；本文件不是已批准的反应网络、自由能表或可执行的 Fe(110) CATKINAS 输入。

## 已确认的本地条件

- 项目指定 CATKINAS 为基线平均场 MKM 工具，配置见 `configs/postprocessing_software.yaml`；`modules/baseline_mkm/README.md` 要求先有守恒的网络、自由能及工况。
- 本机存在 `D:/BaiduNetdiskDownload/bin/matlab.exe`、`C:/Users/86177/Desktop/app/CATKINAS/CATKINAS.p`、`ReadMe.m` 和 `quickstart/INPUT_single.m`。历史 `quickstart/result_single` 有 2026-06-26 的输出；本轮未重新运行，也未验证 Fe(110) 输入。
- 官方示例的运行形式是在 MATLAB 中将 CATKINAS 目录加入路径，再调用 `CATKINAS('INPUT_single')`；项目专用输入应放入独立目录并由受审数据生成，不修改官方 quickstart。
- 截至本次只读查询，`data/project_registry.sqlite3` 有 4 组 `accepted` TS 电子能垒：Fe(110) CO 解离、C*+H*→CH*、CH*+H*→CH₂*、C₂HO*+H*→C₂H₂O*。它们不是完整闭合机理，也不是同一温度下的自由能垒。INT06→MID 的局部频率仍在计算/验证链中，不列为已接受动力学步骤。

## 待用户确定的模型边界

| 选择 | 需要明确的内容 | 当前状态 |
|---|---|---|
| 目标 | 首版限定 Fe(110) 上 C₂H₂O 形成路线；先求相关表面步骤的通量与覆盖度，只有补齐脱附和竞争产物支路后才计算可解释的产品 TOF/选择性 | 用户已选路线；输出边界待网络审核 |
| 工况 | 温度或范围；CO、H₂及主要产物分压；固定分压还是进料/流动模型 | 仅有下文文献启发的探索建议，未批准为正式条件 |
| 表面位点 | 单类 Fe 位点是否足够；桥位、长桥位及多位占据物种如何计数；位点密度的定义 | 待逐个吸附结构核对 |
| 模型边界 | 哪些气相吸附/脱附、H 供给、O 清除、C–C 形成与表面迁移步骤进入首版 | 待反应网络缺口审核 |
| 能量与速率 | 统一温度/标准态的 ΔG、ΔG‡、吸附动力学约定；是否由输入自由能直接给 CATKINAS，避免重复校正 | 尚未具备正式输入 |

## 文献工况与首版探索建议

- 铁基费托单事件微观动力学研究覆盖 `523–623 K`、总压 `0.6–2.1 MPa`、进料 `H₂/CO=2–6`；这是实验铁基催化剂的研究范围，不是金属 Fe(110) 或 C₂H₂O 的已验证最优条件。[Lozano Blanco 等，2008](https://biblio.ugent.be/publication/532104)
- 另一项 Fe₅C₂(510) 的第一性原理微观动力学研究取 `523–623 K`，并设置 `p(CO)=0.83 MPa`、`p(H₂)=1.67 MPa`。其表面是碳化铁、研究对象是费托烃类全网络，不能将结果或位点模型移植到本项目金属 Fe(110)。[Liu 等，2023](https://pubs.rsc.org/en/content/articlelanding/2023/sc/d3sc02054a)
- **推断/待审建议：**首版仅为敏感性探索，取中心点 `573 K`、总反应物压 `2.0 MPa`、`H₂/CO=2`；若进料只含 CO/H₂，则 `p(CO)≈0.667 MPa`、`p(H₂)≈1.333 MPa`。随后扫描 `523、573、623 K`，再检查压力和配比敏感性。此中心点是从上述范围挑选的方便比较值，**并非文献直接给出的 Fe(110) 标准条件**。
- C₂H₂O、H₂O 等产物分压及其是否视为开放气相库仍待网络与边界条件设计；不得把零分压、示例默认值或碳化铁论文的每种产品分压直接当成正式输入。金属 Fe(110) 在合成气中的相稳定性也未由本模型验证。

## 已知网络缺口

当前已接受的四组电子能垒只覆盖 CO 解离、C→CH、CH→CH₂、C₂HO+H→C₂H₂O。要得到从 CO/H₂ 进料到 C₂H₂O 的闭合速率模型，仍需逐项审核 CO/H₂ 吸附与 H 供给、C–C/O 保留路径及其迁移步骤、O/H₂O 去除、C₂H₂O 脱附与逆反应，以及表面位点守恒。此处是**缺口清单**，不是断言这些步骤均无计算记录；正式缺口应以登记库和兼容端点逐条核对。仅凭 C₂H₂O* 形成步骤不能报告气相 C₂H₂O 的 TOF 或选择性。

### 登记库逐步盘点（2026-09-25 只读快照）

此表的方程是**候选网络拓扑**，不是已完成的位点平衡或动力学输入。`*` 的位点占用、覆盖度和同一表面构型兼容性均待审核；尤其不能将独立吸附态能量直接相加替代同胞共吸附端点。

| 候选步骤 | 已有可追溯证据 | 进入正式 MKM 前的缺口 |
|---|---|---|
| CO(g) ⇄ CO* | 气相 CO `fe110_gas_CO_9733110`；CO/top `fe110_ads_co_top_step12a_job9558184`；已接受电子态 | 统一温度/标准态的吸附自由能与吸附/脱附速率约定 |
| H₂(g) ⇄ 2H* | 气相 H₂ `fe110_gas_H2_9573527`，H* 吸附记录存在；已接受 TS 能垒表无此步 | 解离/复合动力学、双位点计数及与反应端点兼容性 |
| CO* ⇄ C*+O* | `fe110_co_dissociation_topic1_9558184_to_9622455`：Grade-A、已接受正向电子能垒 `1.350209 eV` | 热校正、O* 后续去向；不能把该支路自动当成唯一的 C₂H₂O 前驱路径 |
| C*+H* ⇄ CH*+* | `fe110_c_h_to_ch_A9715450_to_9573951`：Grade-A、已接受电子能垒 `0.887692 eV` | IS/FS 热校正、位点约定；当前只是可能的 CH* 供给支路 |
| CH*+CO* ⇄ C₂HO*+* | 化学计量上可作为待检的偶联/插入候选；已接受 TS 能垒表无此步 | 反应原子映射、同构共吸附端点、TS、产物异构体识别及自由能；不得作为已验证基元步骤 |
| IS-A → MID 的 H* 表面迁移 | 已有 VASP CI-NEB `9796856`；`VFA9798421` 在 `tasks/current_task.md` 中仍属待验证链，MID 端点 `9737143` 的化学身份登记为 C₂HO*+H* | 虚频/连通性及兼容能垒正式接受；此步不能被 MID→FS-A 的已接受能垒替代 |
| MID C₂HO*+H* ⇄ FS-A C₂H₂O*+* | `fe110_c2ho_h_hydrogen_transfer_MID9737143_to_FSA9725471`：Grade-A、已接受正向电子能垒 `1.285827 eV`；两个端点有已接受兼容 TOTEN | IS/MID/FS 一致活性集热校正和动力学位点定义；这是局部 O–H 成键段，不代表整条 IS-A→FS-A |
| C₂H₂O* ⇄ C₂H₂O(g)+* | FS-A `9725471` 有已接受兼容电子能；登记库的 `calculations` 未检出 C₂H₂O 气相参考 | 确认表面/气相异构体对应、气相参考、脱附自由能与速率；在此之前不能报告气相产物 TOF |
| O* 清除与 H₂O 释放 | O* 和 H₂O 吸附/气相参考记录存在 | 将 O*→OH*→H₂O*→H₂O(g) 等候选逐步核对为真实基元反应，补 TS/自由能，闭合氧与位点循环 |

另有已接受 `CH*+H*→CH₂*+*` 电子能垒 `0.554547 eV`，但它在首版中是竞争/替代支路证据，不能未经审核并入上述 C₂HO 形成链。**所有上述能垒目前均是电子能垒，不是 573 K 的自由能垒。**

## CATKINAS 最小用法与输入要求

1. 在 MATLAB 中切换到独立工作目录，并加入 CATKINAS 程序目录：`addpath('C:/Users/86177/Desktop/app/CATKINAS')`。
2. 如需运行官方测试，应先把 `quickstart` 复制到独立测试目录，再在副本中运行 `run_single`；它调用 `CATKINAS('INPUT_single')`。这只验证软件，不验证 Fe(110) 科学模型，也不会覆盖原目录的历史示例结果。
3. 项目输入需要：基元反应与 `#`/`#1` 等位点记号、每步正向自由能垒与反应自由能、温度、气相分压/流动边界、初始覆盖度、热力学校正和吸附处理模式。CATKINAS 的 `INPUT` 使用 MATLAB 风格赋值及 `<->` 反应行。
4. 项目已自行计算并输入自由能时，不得同时启用会重复加上 ZPE/熵的 CATKINAS 校正模式。官方示例的数值、`ThermoMode`、`BarrierMode` 和位点约定都不能直接复用；尤其本地 quickstart 对 `BarrierMode=1` 的注释与本地 `ReadMe.m` 的模式说明不一致，项目输入必须单独审核该设置。
5. 正式运行前应检查元素守恒、位点守恒、每步 ΔG‡(正)−ΔG‡(逆)=ΔG(反应)、单位/标准态、输入来源和缺失步骤；运行后检查稳态残差、覆盖度范围、TOF 定义及结果对工况的敏感性。

## 本阶段输出与边界

本阶段的交付是 C₂H₂O 路线范围草案、文献启发的探索工况、软件入口核对及登记库逐步缺口表。未建立项目 `INPUT`，未运行 Fe(110) CATKINAS，也未产生 TOF/选择性。下一步核对 FS-A 的结构异构体与候选网络位点占用，再决定哪些步骤可构成首个守恒的表面反应子网络；文献工况仍只是待审模型假设。
