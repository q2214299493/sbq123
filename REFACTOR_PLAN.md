# 重构计划

## 目标

在不改变科学结论、计算参数、数据格式、数据库 Schema、CLI、配置字段和
历史结果的前提下，降低局部耦合并补齐外部命令和文件写入边界。

## 明确不修改

- 锁定的 Fe(110) 兼容分支和所有科学阈值；
- 吸附候选数量、化学判断、NEB/TS 判级顺序和默认参数；
- AQCat25/VASP 执行权限与人工审核门；
- 注册表 Schema、已有数据和历史计算目录；
- 未经确认的疑似科学逻辑错误；
- 未跟踪且仍在开发的 TS endpoint 文件布局。

## 阶段

### A. 基线与审计

- 记录 Git 工作树、代码规模、依赖、导入图、异常和外部命令边界。
- 运行完整 pytest 和 Ruff。
- 输出 `PROJECT_AUDIT.md`。

状态：完成。初始基线为 197 项测试中 1 项结构契约失败，Ruff 通过。

### B. 低风险清理

- 将执行决策的纯序列化从授权分支中拆出。
- 为当前 SSH、LSF 查询和 NEB 外部命令增加有限超时。
- 修复原子 JSON 固定临时文件名。
- 增加对应回归测试。

状态：完成。没有删除代码、修改公开接口或改变决策 Schema。

### C. 结构优化

- 先为 VFA、NEB 输出分析和路径质量的长函数补足输入/输出测试。
- 每次只拆一个“解析—判定—报告”边界。
- 稳定 TS endpoint 实现后，再决定是否从 `modules/` 根迁入 `scripts/` 包。

状态：暂缓。当前工作树改动密集，继续移动文件会显著增加冲突和回归风险。

### D. 边界与可靠性

- 为缺少 Schema 的高风险配置增加映射类型和必填字段验证。
- 审核直接文件写入点的覆盖、幂等和断点续跑语义。
- 为 AQCat25/FairChem GPU 环境提供独立依赖锁定，不污染基础包。

状态：部分完成。外部命令超时和共享 JSON 写入已处理。

### E. 最终验证

- Python 语法检查；
- 修改范围 Ruff；
- 相关模块测试；
- 完整 pytest；
- CLI `--help` 和配置解析 smoke；
- Git diff 与用户原有改动隔离检查。

状态：完成。Ruff、202 项完整回归、29 个配置文件解析、两个主 CLI
`--help` 和修改范围 diff 检查均通过。

## 优先级

1. 保持执行门和外部副作用安全。
2. 保持科学语义和数据兼容。
3. 修复已有测试失败。
4. 只拆分有测试保护且职责明确的代码。
5. 不以文件数或行数减少作为独立目标。

## 完成标准

- 完整测试无失败；
- Ruff 无新增问题；
- `execution_gate.py` 不超过结构契约上限；
- 所有本次涉及的外部命令具有有限超时；
- 原子 JSON 写入不会共享固定临时文件；
- README、架构、审计、退役和重构报告与当前实现一致。
