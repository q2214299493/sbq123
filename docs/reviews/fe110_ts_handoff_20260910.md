# Fe(110) C₂HO* + H* → C₂H₂O* 任务交接

更新时间：2026-09-10（Asia/Shanghai）

工作区：`C:\Users\86177\Desktop\work`。下述相对路径均以此为根目录。
本文是交接索引，不替代实时调度、原始计算文件、数据库或哈希绑定执行门。
本次只整理文档；未查询集群、提交计算、修改生产数据库或重新接受科学结果。

## 1. 接手先看结论

- 目标是末端 **O 接受新增 H**，不是 C 加氢：H–C–C–O 基团 + H* → H–C–C–OH 基团。星号代表整个吸附态，不据此断言只有 O 与 Fe 成键。
- **MID9737143 → FS-A9725471 的 O–H 成键 TS 已正式接受，电子能垒、策略模板和课题表均已登记。不要重跑这部分。**
- 尚未完成的是 **原始 IS-A → MID 的表面 H 迁移段**。此前 GPU 路径出现两个迁移峰，不能仅凭双峰认定已有两个真实 TS。
- 当前待跟进作业是 **9748648：迁移路径 valley image 06 的独立普通 VASP 松弛**，不是新 NEB 或 Dimer。用途是判断该谷点是否保持为独立稳定中间态。
- `tasks/current_task.md` 只保存提交时的 `PEND`，**不是当前实时状态**；本交接未查询其是否开始或结束。

## 2. 已完成的局部 O–H TS

结果来源：`docs/reviews/oh_ts_accepted_barrier_20260910.md`，正式证据目录见第 4 节。本次核对了该审核记录，未重新运行科学验证。

| 项目 | 已登记结果 |
|---|---|
| Dimer 作业 | 9746548，已正常/电子收敛完成 |
| TS 最大原子力 | 0.018201 eV/Å |
| 曲率 | −11.8812 eV/Å² |
| 显式接受的残差 | DIMCAR Force 0.06398、Torque 1.2054；不与最大原子力混为一谈 |
| 局部频率作业 | 9747902 |
| 局部 Hessian | O48/H50，零基索引 47/49，6 个自由度 |
| 唯一虚频 | 1407.654162 cm⁻¹，mode 6，已归属 O–H 反应坐标 |
| 审核结果 | TS_ACCEPTED，按现有规则显式审核登记 Grade A |

原频率专用批准与原始文件保留；用户随后接受当前 Dimer 残差用于正式 TS 验证。此接受仅针对本次证据，不可作为其他任务的自动残差豁免。

| 状态 | 最终 OUTCAR TOTEN / eV |
|---|---:|
| 局部 IS = MID9737143 | −388.43796188 |
| TS9746548 | −387.15213470 |
| FS-A9725471 | −388.04695175 |
| 正向电子能垒 | **1.28582718** |
| 反向电子能垒 | **0.89481705** |
| 反应能 | **+0.39101013** |

这些差值只对应 MID→FS，不是原始 IS-A→最终 FS 的完整过程能垒。
只验证了局部 Hessian，未证明全体系仅有一个虚频；未包含 ZPE、熵或自由能修正，不能称为动力学就绪数据。

兼容分支：PBE / 相同 PAW-PBE Fe、C、O、H 势，ENCUT=400 eV，Gamma 5×5×1，ISPIN=2，相同磁性初始化，ISMEAR=1，SIGMA=0.20 eV，相同晶胞和底部 18 个 Fe 固定。
能量约定：`fe110_converged_toten_sigma0p20_v1`。不得混入 SIGMA=0.10 eV 或 ML 预测能量。

## 3. 登记已完成，不要重复插入

- 数据库：`data/project_registry.sqlite3`；先前验证为 schema 9。
- TS ID：`fe110_c2ho_oh_dimer9746548_vfa9747902_accepted_20260910`
- 电子能垒 ID：`fe110_c2ho_oh_mid9737143_ts9746548_fs9725471_sigma0p20`
- 策略 ID：`fe110_c2ho_oh_local_micro_neb_dimer9746548_grade_a`
- 课题表：`outputs/oh_ts_barrier_20260910/课题一TS.xlsx`，`TS记录` 第 6 行。
- 原先所说的 **35 条**是：2 条计算档案 + 2 条作业 + 2 条状态事件 + 27 条文件索引 + 2 条审核。不是 35 个 TS，也不是 35 套能垒；正式 TS/能垒是在随后接受后追加登记的。

登记时已核对数据库完整性、外键、TS/能垒/模板关联；Excel 原有三张表的值和公式保留，新行公式与能量一致，45 项相关测试通过。这是已执行检查的记录，不是本交接重新测试的结果。

## 4. 最小文件入口

| 用途 | 路径 |
|---|---|
| 接手规则 | `AGENTS.md` |
| 当前执行步骤 | `tasks/current_task.md` |
| 后端权限 | `configs/execution_backends.yaml` |
| TS 方法和执行门 | `modules/transition_state_search/README.md` |
| 正式 O–H 审核摘要 | `docs/reviews/oh_ts_accepted_barrier_20260910.md` |
| 原 35 条登记说明 | `docs/reviews/oh_ts_backfill_20260909.md` |
| 当前反应目录 | `calculations/fe110_c2ho_h_to_c2h2o_ts_20260822/` |
| 正式 TS、能垒、执行门和登记回执 | 上述目录下 `formal_ts_validation_20260910/` |
| 原 Dimer 文件 | 上述目录下 `dimer_job9745217_image02_20260907/`（目录名称含父作业号，不要误当成最终 Dimer 作业号） |
| 原局部频率文件 | 上述目录下 `vfa_dimer9746548_local_oh_20260909/` |
| 待跟进松弛输入包 | 上述目录下 `h_migration1357_valley06_relax_20260909/` |

需精确输入/输出哈希时直接读取正式目录的 manifest、gate 和 receipt，禁止从聊天重建哈希或改写原文件。

## 5. 下一步与授权边界

接手后的唯一优先科学步骤：**用户要求继续或查询进展时，检查现有 9748648 一次。**

1. 分别核对调度状态、电子收敛、离子/力收敛和最终几何；不能把 DONE 当作收敛。
2. 若结束，判断 valley image 06 是否仍为独立吸附中间态，是否滑向相邻盆地；检查同一个 H50 的迁移、周期映射、固定层和化学连接。
3. 根据真实结果再确定原 IS-A→MID 的分段与峰值审核，不预先承诺一定是两段独立 TS。
4. 只有满足所属模块证据门，且获得对应用户授权，才能提交后续 NEB/Dimer/频率计算。

当前交接**不授权**额外提交、重投、终止、延长作业或恢复定时监控。现有包的远程路径/调度命令从其登记与配置读取，不猜测。
VASP/VTST 仅用 `sunboquan-codex`；MZ73 的 ML/GPU 角色按当前后端配置执行。GPU 候选必须回到本地审核，不能直传后自动提交 VASP。

## 6. 保留的用户方法偏好

- 成功且符合门禁的吸附结果、TS、电子能垒和可复用策略应及时登记，幂等检查，不等用户再次提醒。
- 方法按反应与路径证据选择，不强制每次都走相同 NEB/CI-NEB/Dimer 顺序。
- GPU 路径失败时，应解释主动学习与 VASP 局部验证两条选项。仅失败本身不证明模型失准；同结构 DFT 能量/力误差是微调依据。
- 保持训练/筛选与 held-out 数据隔离，吸附态 replay 用于抑制微调后的吸附性能退化。不能只凭双模型分歧宣称定量不确定性或已完成校准。
- 只复用方法经验，不复制原子索引、坐标、MODECAR、能垒或本次 O–H 阈值。轻微边界异常结合连续性等证据评估，不新增绝对化数值硬门。
- 以上为方法偏好；下一次执行须核对当前 checkpoint、数据清单及门禁，本文不宣称任何新 GPU 包已获授权或可直接运行。

## 7. 本轮已修复的额外返工

已写入 `AGENTS.md`、`skills/context_safe_shell.md` 和登记模块 README：

- 同一轮复用已读的未改变内容；截断后只补读缺失部分；不倾倒整个脏工作区列表。
- 先查真实路径、CLI、数据库字段和状态枚举，不猜测试错。
- 使用规范文件输出，不从终端字符串截取 JSON；哈希绑定证据不跨 JavaScript/PowerShell 重序列化。
- 一次计划同批合格记录；保留依赖的科学和 Excel 门禁；失败先查原因及回执再重试。
- 进展/解释请求保持只读，不顺手开展历史补登记或无关修复。

代码：`scripts/registry_write.py` 摘要增加 `plan_sha256`，避免误用 batch hash；终端采用 ASCII 转义，保存文件仍为 UTF-8。
测试：`tests/test_registry_write_cli.py` 覆盖 plan/apply、文件/终端输出、Unicode、计划哈希和浮点保持；语法、Ruff、74 项相关测试通过。
本地提交 `ff4b3ba`，已发布到 `sbq123` 的提交 `1d8b3bb`；正式 TS 登记此前发布为 `320b01e`。

## 8. 已知阻塞及不可覆盖内容

`repo-state audit --phase start` 本次仍报两个未被取代的当前任务事件冲突：

- `task-completed-966f70761e2de2c804e6dced`
- `task-migration9748648-open-20260909-v2`

这阻止状态摘要安全同步，不等同于科学登记失败。需通过 `state_handoff` 的审阅/协调流程处理，不能删除或原地修改事件，也不能让已完成 O–H 任务覆盖仍开放的迁移任务。
`docs/02_CURRENT_STATE.md` 含旧 schema/状态摘要，不能据其否定后续已登记结果。数据库与原始证据优先。

工作区存在大量其他任务的改动，必须保留。仅提交本任务所属文件；默认发布目标是 `sbq123` 的独立历史，不是 `origin/sbq`。
源码已发布不代表大型运行证据、数据库及 Excel 都有异机备份；本交接未审核完整备份。

## 9. 可复制给接手者的指令

> 请先读取本交接及 tasks/current_task.md，遵循 AGENTS.md 并运行只读 repo-state audit。已登记的 MID→FS O–H TS 不要重复计算或登记。用户要求继续时，先检查现有作业 9748648，并根据独立谷点松弛结果决定后续 H 迁移分段。任何新的计算须核对当前证据门和用户授权。保留现有状态事件冲突，不擅自覆盖；按已修复的紧凑读取、规范 JSON 和批量预检规则执行。
