# 用户授权范围（2026-09-26）

本轮用户原文：

> 记录此次结果为grade A 然后 以这条收敛 VASP 路径初始化一次受限 GPU ML‑NEB，先检验路径优化是否仍会偏离；若仍失败，再对偏离处做同构 VASP 标签比较，决定是否微调。

执行范围：一次 40 步上限、七图像、MatRIS epoch6 主模型的普通 GPU ML-NEB；不做 ML-CI，不自动重试，不自动微调。AQCat25 对最终同构路径审计。

本轮 GPU 测试未失败，故条件性的新增 VASP 标签/微调分支未执行。Grade-A 正式入库请求仍受非 Dimer 科学验收规则约束，尚未登记为 Grade A。
