# State Handoff Archive

This directory accepts only user-approved, unique, previously untracked
repository material. Every archived item must have an adjacent manifest with
its original path, SHA-256, reason, proposal ID, and review event ID.

It must not contain credentials, POTCAR, VASP runtime outputs, calculation
trees, caches that can be regenerated, or files larger than the configured
archive limit.
