"""Transition-state endpoint implementation package.

The owning scientific protocol remains in ``modules/transition_state_search``.
This package contains implementation only and intentionally has no eager
re-exports, so importing it does not load geometry or registry dependencies.
"""
