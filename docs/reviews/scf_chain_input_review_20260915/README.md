# 三阶段 SCF 输入包：待用户审核与提交授权

2026-09-15。本轮“继续”执行真实输入准备和不运行 VASP 的预检。
没有远端目录创建、输入落盘上传、MPI 计算、VASP 提交或自动重投。

## 结论

输入包已准备，canonical `scf_repair_chain` 预检通过；三份 INCAR 的
custodian 静态输入检查均无 blocker。远端 Python 3.14.5 已实际编译包内
11 个 Python 文件/入口，`bash -n` 检查脚本通过，均未执行计算。
该结果不是三阶段 SCF 成功；当前执行门为 `NEEDS_SCF_CHAIN_SCOPE`，
`ALLOWED_ACTIONS=[]`，没有提交授权。

权威本地目录：
`calculations/fe110_c2ho_h_to_c2h2o_ts_20260822/h_migration_int06_mid_scf_chain_20260915`。
计划远端目录（本次检查不存在）：
`~/sbq/Fe110/ts/c2ho_h_to_c2h2o_20260904/h_migration_int06_mid_scf_chain_20260915`。

## 输入与阶段

同一结构：Fe45 C2 O1 H2，共 50 原子；零基 0–17 固定。POSCAR 与已完成
9752745 的输入逐字节相同，也与 9755394 相同；原几何审核证据哈希有效。
复用已验证的本地结构，不引入外部结构或重新构造路径。

PBE、ENCUT=400 eV、Gamma 5×5×1、ISPIN=2、ISMEAR=1、SIGMA=0.20 eV、
LREAL=False、LASPH=True、LDIPOL=False、ISYM=0 均保持不变。
原有 50 个 MAGMOM 初值与 9752745 最终局部磁矩一致，保留原近线性混合参数。

| 阶段 | ISTART / ICHARG | 相对 9755394 的改动 |
|---|---|---|
| A 固定原子密度诊断 | 0 / 12 | 新增 NCORE=8、显式 NBANDS=320 |
| B 自洽冷启动 | 0 / 2 | 上述并行设置，另恢复自洽 ICHARG=2 |
| C 从 B 重启复现 | 1 / 1 | 上述并行设置，读取 B 已验证的电子态 |

三个阶段都用 80 MPI ranks、NSW=0、IBRION=-1、ALGO=Normal、
EDIFF=1e-7、NELM=200、LMAXMIX=4。不是重新优化结构，也不是 Dimer。
NBANDS=320 与旧作业实际值一致；NCORE 从旧实际值 1 改为 8 是待验证的
主要并行数值对照，不能事先断言它必然修复根因。

A 的文件不进入 B；只有 B 的 WAVECAR/CHGCAR 通过检查，C 才能启动。
结果失败只阻止后续阶段，不自动增加其他参数试验。

## 基线与候选比较阈值

基线是 9752745 原始 OUTCAR/OSZICAR，不是手填能量或复制失败结果。
所有来源哈希均回查；电子收敛、正常结束、50 个完整力矢量及局部磁矩均已复核。
TOTEN=-388.28288317 eV，总磁矩=105.0208746 μB，45 个 Fe 局部磁矩均为正。
它是当前同结构的已知成功 SCF 参考，不代表已证明全局最低磁性解。

以下仅是本次固定几何数值恢复的**待用户审核阈值**，不是通用物理标准，
也不能用于代替 TS 力、虚频或能垒审核：

| 比较量 | 相对基线：警告 / 停止后续 | C 相对 B：警告 / 停止后续 |
|---|---|---|
| 总能差绝对值（eV） | 0.010 / 0.050 | 0.001 / 0.010 |
| 最大逐原子力矢量差（eV/Å） | 0.010 / 0.050 | 0.005 / 0.020 |
| 最大局部磁矩差（μB） | 0.10 / 0.30 | 0.02 / 0.10 |
| 总磁矩差（μB） | 0.50 / 2.00 | 0.10 / 0.50 |

超过警告线、但未超过停止线，记录软警告并允许进入下一阶段。
这些数值留出输出舍入及小幅数值差异空间，主要用于拒绝明显偏离参考状态
或不能重启复现的结果；尚未通过新计算标定。超限不单独证明发生了磁性分支切换。

几何输出舍入容差为 2e-5 Å；已知基线 OUTCAR 打印精度导致的表观差约
7.88e-6 Å。输入坐标、晶胞和固定层本身仍严格由文件哈希绑定。
这里比较的是相同几何的**力矢量之差**，不是要求静态结构的绝对力趋于零；
基线约 0.353 eV/Å 的最大原子力不能被标成 TS 已收敛。

## 资源与风险

一次排队申请 80 核，不是同时申请 240 核；保持排除 gknew0440。
每阶段最多 10800 s（3 小时），最多三阶段，总执行预算上限 9 小时、
720 核时，不包括排队，且不是预计耗时。该上限参考基线 96 电子步约
3836 s，并为最多 200 步留余量；新并行设置的实际速度未知。
前段失败不会继续花费后段预算；超时是资源终止，不应误称物理不收敛。

远端 MPI 为 Intel MPI 5.1，已绑定启动器哈希；VASP 二进制与原记录一致，
在已加载 Intel 环境后 ldd 未发现缺失库。POTCAR 远端哈希与原授权分支一致。
这些检查只覆盖登录节点及可见共享文件，尚未在新计算节点验证 MPI 启动、
LSF 环境变量传递、真实自洽恢复和 B→C 电子态重用。

旧作业 9755394 当前调度 DONE；原始日志尾部报告 EDDDAV/ZHEGV，
WAVECAR/CHGCAR/CONTCAR 均为空。它不是恢复成功，也不提供可用重启态。

## 冻结标识与下一步

- manifest SHA256：`c7b3adc5590257d966c81124a22f9dc5ccfed582588f311ce0cb1081f3468796`
- bundle SHA256：`58cd3fde093f83b58beea427c7f30e9347227d044638cab4604b5fc805fe32ba`
- runtime SHA256：`82fb87b43902c7dccfd72a9d4d78f40b2cfe2aa24f22803157f96b6270fc1111`

任何输入、阈值、运行时代码或资源脚本改动，都需要新哈希及新审核。
发布库与工作区代码组织不同，禁止在发布分支随意重建 runtime 后套用上述哈希；
部署须使用本次在 work 冻结的包。

下一步：用户审核上述阈值和资源上限，并单独授权提交该三阶段包后，
才生成当前 `scf_chain_scope` 与 canonical 提交门。C 通过也只表示数值恢复
和重启复现通过；是否继续短 Dimer，仍须单独审核与授权。
