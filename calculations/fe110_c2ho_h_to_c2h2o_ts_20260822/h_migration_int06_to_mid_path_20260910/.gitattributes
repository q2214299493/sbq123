# Preserve hash-bound evidence bytes, including Windows-generated inputs.
** -text whitespace=cr-at-eol
# Preserve native VASP/VTST formatting in scientific geometry evidence.
*.vasp -whitespace
**/POSCAR.xyz -whitespace
**/movie -whitespace
**/movie.xyz -whitespace
