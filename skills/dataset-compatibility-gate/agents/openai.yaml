interface:
  display_name: "Dataset Compatibility Gate"
  short_description: "Check VASP dataset branch compatibility."
  default_prompt: "Check whether these VASP results can be compared or promoted into the current dataset."
