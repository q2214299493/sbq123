# 前段 H 迁移继续方案与准备结果

日期：2026-09-09。状态：输入准备完成，未提交新作业；不是两个 TS 已完成。

## 本次提交授权后的检查

用户已明确批准提交 image06 一个普通松弛任务。未提交，尚无 job ID。
输入和远程 POTCAR 哈希核对通过，目标目录不存在。
已为统一执行器补充 adsorption_relaxation 适配，调用现有吸附预检，
不改科学阈值、不绕过授权和重复提交保护。语法、Ruff及112项相关测试通过。
实际生产预检在读取失败历史时被阻塞：代码要求数据库 schema9，
生产库仍为schema8；只读 quick_check=ok。这不是结构或VASP收敛失败。
尚未创建提交reservation，未执行bsub或数据库迁移。
下一步需要单独批准备份审查后的生产库8→9升级；本次单作业提交授权保留。

## 已验证的来源与范围

- 复用 GPU 1357 的 IS-A9725473 → MID9737143 九图像最终路径，不改动已有 MID → FS-A 的 O–H 分支。
- 9/9 最终 POSCAR 与当前完整路径 manifest 中的结构哈希一致。
- 当前 manifest SHA256：ac49dfa1673bdfe26a762686ddb7f6681086a81b06c24314f49537c04921507e。
- 原子序列 Fe45 C2 O H2；固定零基 0–17；迁移的是同一个 H50（零基49）。
- 独立最小镜像检查：最大相邻可动原子 RMSD 0.139440 Å，最大单原子跨步 0.768873 Å。未发现由单胞跳转造成的异常大跨步；这不证明 DFT 路径或 TS。
- C–C 1.415161–1.419370 Å，C–O 1.280884–1.288203 Å，原有 C–H 1.097716–1.100368 Å。
- 运行 VTST dist.pl（全部8段）和 nebmovie.pl 1，命令退出0；生成 movie，30080 bytes。只使用完成的 ML 结构复制件，CONTCAR 文件名为工具兼容，不代表 VASP 结果；动画不含 VASP 能量。
- 注册库中现有同反应6个终态结构与06比较，身份映射下均非相同结构；尚未做所有表面对称操作下的等价类穷举。

## 关键峰谷（仅 MatRIS 预测）

能量以1357 image00为零；不是 VASP 电子能垒。

| 图像 | 相对预测能量/eV | O–H/Å | 解释 |
|---|---:|---:|---|
| 00 | 0.000000 | 3.079728 | 原始 IS-A |
| 02 | -0.074005 | 3.589807 | 第一峰之前的另一个局部低点，不能忽略 |
| 03 | 0.080371 | 3.512725 | 第一个迁移峰候选 |
| 06 | -0.064468 | 3.804939 | 03与07之间的最低图像 |
| 07 | 0.084853 | 3.073491 | 第二个迁移峰候选 |
| 08 | 0.024986 | 2.669336 | 已有 MID |

不能由两个 ML 极大值直接认定恰好两个基元反应。原始 IS-A 与 image02 的关系也需后续检查。

## 当前唯一准备执行的步骤

对 image06 做一次独立、无额外约束的 VASP 普通松弛，检验峰间中间态是否存在。

- 包：`calculations/fe110_c2ho_h_to_c2h2o_ts_20260822/h_migration1357_valley06_relax_20260909`
- POSCAR 与源图像06完全相同，SHA256：40a64036a5bf13aa5a30729c53902f94126438e19685d1bfe2096d1396826a43。
- 保留原子顺序、晶胞及底18个Fe固定；无新加 H/键长 restraint。
- 官方 routine_production builder：PBE/ENCUT400/Gamma5×5×1/ISPIN2/ISMEAR1/SIGMA0.20。
- EDIFF=1e-5；EDIFFG=-0.02 eV/Å；IBRION2；POTIM0.20；NSW300；32核。
- 官方 adsorption preflight PASS，0 errors、0 warnings。
- 未上传、未提交；尚无 job ID。远程 POTCAR 核对、当前提交授权和执行门仍待提交阶段办理。

## 结果决定后续路线

1. 若06独立松弛后保持新的稳定共吸附态：完成兼容性/几何/收敛验证和自动登记，作为共同端点；分别审核 IS-A→新中间态及新中间态→MID，并处理00–02局部低点。
2. 复用1357的局部峰值区域和周期映射，不自动复用旧 MODECAR。审核准确端点及峰值±邻居的同构 VASP 能量/力证据后，再选择局部 Dimer 或必要的短 NEB。
3. 若06滑入已有态：不把它登记为新的稳定中间态，不硬拆两个TS；按 VASP 结果重新确定迁移分段。
4. 如出现模型失败，先区分几何问题和同构 VASP 力误差。给出主动学习与局部 VASP 两个选项；只有误差证据支持时才微调，不因不收敛或模型间分歧自动训练。
5. 每个实际获得的TS仍须独立频率/方法验收、兼容电子能垒、课题表及策略登记。ML峰、输入预检和调度DONE都不能代替这些环节。

## 保留的不确定项

- 尚无 image06 独立 VASP 最小值，因此两个前段迁移TS均未被验证。
- 02的局部低点可能影响第一步定义，不能用现有ML曲线排除额外亚稳态。
- 已有 O–H 频率分支的正式验收/登记是独立待办，不被本次迁移准备覆盖。
